import {Helmet} from 'react-helmet';

const UpdatePropile = () => {
    return (
        <div><Helmet>
        <title>Update propile</title>
        <meta name="description" content="Helmet application" />
    </Helmet>
            <h1>Update propile page</h1>
        </div>
    );
};

export default UpdatePropile;